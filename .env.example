# Canonical public origin used for sitemap, canonical links and structured data.
SITE_URL=https://chassis-index.pages.dev

# Optional. Analytics is omitted from the HTML when this value is empty.
PUBLIC_GA_ID=
